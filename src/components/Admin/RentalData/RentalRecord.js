import React from 'react';

function RentalRecord({ location }) {
  // const [rentalData, setRentalData] = useState(null);

  return <div>This is a single review</div>;
}

export default RentalRecord;
